<?xml version="1.0" encoding="utf-8"?>
<!--
    This is the layout for the password reset dialog.
    It's a simple layout with an email input field and a message.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- A simple message to the user. -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Enter your email address and we'll send you a password reset link."
        android:textColor="@color/white"
        android:textSize="14sp"
        android:layout_marginBottom="16dp" />

    <!-- An input field for the user to enter their email address. -->
    <com.google.android.material.textfield.TextInputLayout
        style="@style/AuthEditTextStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:boxStrokeColor="@color/auth_accent"
        app:hintTextColor="@color/auth_hint">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etResetEmail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Email Address"
            android:inputType="textEmailAddress"
            android:textColor="@color/white"
            android:textColorHint="@color/auth_hint" />

    </com.google.android.material.textfield.TextInputLayout>

</LinearLayout>