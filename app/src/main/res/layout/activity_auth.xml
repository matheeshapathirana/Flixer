<?xml version="1.0" encoding="utf-8"?>
<!--
    This is the main layout for the authentication activity.
    It's a simple FrameLayout that acts as a container for the auth fragments.
    The background is a Lottie animation to give it a cool cinematic feel.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/fragment_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_cinematic_gradient">

    <!-- A Lottie animation for the background to give it that cinematic vibe. -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/backgroundAnimation"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        app:lottie_autoPlay="true"
        app:lottie_loop="true"
        app:lottie_rawRes="@raw/cinematic_film_grains" />

    <!-- A semi-transparent overlay to make the text more readable. -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/gradient_overlay" />

</FrameLayout>