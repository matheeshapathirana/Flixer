<?xml version="1.0" encoding="utf-8"?>
<!--
    This is the layout for the welcome screen, the first screen the user sees.
    It's a simple layout with a Lottie animation, the app title, and buttons
    to either log in or sign up.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="@android:color/transparent"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:layout_gravity="center_vertical">

        <!-- A fun Lottie animation to give the app some personality. -->
        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/animationView"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_marginTop="40dp"
            app:lottie_autoPlay="true"
            app:lottie_loop="true" />

        <!-- The app's title, big and bold. -->
        <TextView
            android:id="@+id/appTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="FLIXER"
            android:textColor="@color/white"
            android:textSize="36sp"
            android:textStyle="bold"
            android:letterSpacing="0.05"
            android:layout_marginTop="16dp"
            android:textAllCaps="true" />

        <!-- A catchy tagline to draw the user in. -->
        <TextView
            android:id="@+id/tagline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Your cinematic universe awaits"
            android:textColor="#CCFFFFFF"
            android:textSize="14sp"
            android:textStyle="italic"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="40dp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="20dp">

            <!-- The main call to action for existing users. -->
            <Button
                android:id="@+id/btnLogin"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:text="Log In"
                android:textAllCaps="false"
                android:textSize="16sp"
                android:textStyle="bold"
                android:background="@drawable/button_majestic_glow"
                android:textColor="@color/white"
                android:letterSpacing="0.02"
                android:layout_marginBottom="16dp"
                android:visibility="visible" />

            <!-- The main call to action for new users. -->
            <Button
                android:id="@+id/btnSignUp"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:text="Sign Up"
                android:textAllCaps="false"
                android:textSize="16sp"
                android:textStyle="bold"
                android:background="@drawable/button_glass_outline"
                android:textColor="@color/white"
                android:letterSpacing="0.02"
                android:visibility="visible" />

        </LinearLayout>

        <!-- A little extra text at the bottom. -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🎬 Unlimited Movies &amp; Series"
            android:textColor="#80FFFFFF"
            android:textSize="12sp"
            android:layout_marginTop="60dp"
            android:layout_marginBottom="20dp" />

    </LinearLayout>

</ScrollView>